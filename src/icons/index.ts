/**
 * VDSL icons — public module surface.
 *
 * A curated, offline, inline-SVG icon set (Lucide, ISC). Everything here is
 * pure data + string helpers: no DOM, no fetch, no external assets — so the
 * generated player stays a single self-contained HTML file under a strict CSP.
 */
export { LUCIDE_ICONS, ICON_VIEWBOX, getIcon, hasIcon } from "./lucide";
